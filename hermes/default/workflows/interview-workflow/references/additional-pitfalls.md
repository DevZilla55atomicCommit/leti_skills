# Additional Pitfalls & Fixes

## Pitfall: Misaligning metrics definition
**Fix**: Record exact metrics such as pipeline stages, deep-work hours, and watch time so `/today` and `/closeday` can surface them appropriately.

## Pitfall: Duplicate pitfall entries
**Fix**: Consolidate pitfall definitions to avoid redundancy; ensure each pitfall is unique and clearly scoped to a user preference dimension.

## Pitfall: Overlooking quick‑path context
**Fix**: Even when using quick path, still capture full preference set to maintain downstream configuration accuracy.