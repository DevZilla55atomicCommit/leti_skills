# Session 2026-07-26: Tag-Aware Pipeline Complete & Critical Fixes

## Summary
Processed 771 reels (380 Video Effects + 391 Photography/Videography) with tag-aware extraction pipeline. Key fixes applied and lessons learned.

## Critical Fixes Applied

### 1. Frame Directory ID Mapping (FIXED)
**Problem**: Frame directories use downreels.com internal IDs (e.g., `AQM-utMogMdFAjiQbB49g0Xw6ewUBer8DzROMvYK0Bx1xrjm0yyxYpU_pyQeKJG6mVXlRFPd5JHVRZTTAFFVZ4CBIyrsPr3n6zFPyUE`), but manifest mapped to Instagram short IDs (e.g., `C-5PYQSADOG`).
**Fix**: Updated manifest's `frames_dir` field using download-order mapping from `completed.json`. Now `manifest[reel_id].frames_dir` points to correct downreels.com ID directory.
**Verification**: `manifest[reel_id].frames_dir` now resolves to actual frame directory for vision analysis.

### 2. Skip Patterns Too Aggressive (FIXED)
**Problem**: `lightroom`, `mobile`, `snapseed`, `vsco`, `picsart`, `facetune`, `airbrush`, `meitu`, `beautyplus`, `magimir` in skip_patterns caused valid Lightroom/mobile editing tutorials to be skipped.
**Fix**: Moved these keywords to `lightroom_mobile_keywords` routing to `Photography/Lightroom/` instead of skipping. Camera theory keywords (`raw vs log`, `dynamic range`, `sensor`, etc.) route to `Camera Theory/` instead of skipping.

### 3. GIF Copy Before Cleanup (ENFORCED)
**Bug**: GIFs generated but not copied to vault before `cleanup_reel()` deleted temp files.
**Fix**: All pipeline scripts now copy GIF to vault BEFORE calling `cleanup_reel()`. Verified: 529 GIFs in vault vs 0 before fix.

### 4. Vision Rate Limit (HARD CONSTRAINT)
**Constraint**: 20 RPM maximum, 3s minimum between calls. Enforced in all vision analysis stages.
**Implementation**: `await asyncio.sleep(3)` between each vision call, with exponential backoff on 429.

### 5. Frame Directory ID Mapping
**Learning**: Frame folders use downreels.com internal IDs. Manifest's `frames_dir` field provides correct path. Always use `manifest[reel_id].frames_dir` for vision analysis, never reel_id directly.

## Background Processing Pattern (VALIDATED)
```python
terminal(background=True, notify_on_complete=True, timeout=7200)
# Monitor with:
process(action="poll", session_id=...)
process(action="log", session_id=..., limit=50)
```
Proven: 771 reels processed in background with notify_on_complete.

## Storage Monitoring (CRITICAL)
- `df -h` before each batch
- Temp must be <100MB after cleanup (`du -sh temp/`)
- Vault at `/Users/alfredkamisese/TamaZila Obsidian Vault/Hermes Agent/DaVinci_Knowledge_Base/`
- 120GB Samsung LED SSD: ~70GB free baseline, temp hits ~7.2GB during batch

## Auto-Compression Fix (2026-07-25)
**Root Cause**: `auxiliary.compression.base_url` pointed to local Ollama but model only exists on NVIDIA API.
**Fix**: `base_url: https://integrate.api.nvidia.com/v1` with `provider: nvidia`

## Skill Auto-Install
Copy generated skills to `~/.hermes/skills/creative/` for immediate discoverability via `hermes skills list | grep davinci-reel`

## Pipeline Status (2026-07-26)
- Video Effects: 334/368 reels processed (91% frame extraction rate), 334 skills + 737 notes
- Photography/Videography: 391 reels, 334 skills created
- Total: 1408 Hermes skills, 2378 vault notes
- 36 reels without frame dirs (download/extract failed) - cannot process without re-download

## Vault Structure
```
/TamaZila Obsidian Vault/Hermes Agent/DaVinci_Knowledge_Base/
├── Video_Effects/
│   ├── transitions/ (334 skills, 737 notes)
│   ├── compositing/
│   ├── motion-graphics/
│   ├── vfx/
│   ├── stylization/
│   ├── text-effects/
│   └── time-effects/
└── Photography_Videography/
    └── portrait/ (334 skills, 214 notes)
```

## Remaining Work
- 36 reels missing frame dirs need re-download/re-extraction
- Continue Photography pipeline for remaining subcategories (landscape, commercial, street, lighting)
- Automate index cross-linking (5 index files need updating)