# Instagram Reel Pipeline - Session Reference

## Session Context
- **Date**: 2026-07-19
- **User**: Alfred (Maddie)
- **Profile**: default (Hermes on Mac mini "Leti")
- **Pipeline State**: 120/214 reels processed, 94 remaining

## Key Research Findings (This Session)

### yt-dlp Nightly (Post PR #17075)
- **Fixed**: "Instagram sent an empty media response" (Issue #17074)
- **Fixed**: 404 on reels accessible in browser with cookies (Issue #17124)
- **Requirement**: Browser impersonation + new GraphQL endpoint for logged-out extraction
- **Command**: `pip install -U --pre yt-dlp` → `yt-dlp --cookies-from-browser firefox "URL"`

### Playwright Parallel Contexts
- Single browser process supports 4-6 isolated `BrowserContext` instances
- Each context = independent cookies, cache, storage
- 4x throughput for zero-auth Method B
- Video recording via `page.record_video()` + local ffmpeg frame extraction

### Rate Limits (Residential IP)
| Metric | Limit |
|--------|-------|
| Browser RPM | ~35 requests/minute |
| yt-dlp (authenticated) | ~30-50 reels/minute |
| Batch pause | 60s per 10 reels |
| Cookie lifetime | ~20 hours / 500 reels |

### Pipeline Architecture (Hybrid)
```
Primary: yt-dlp nightly + cookies.txt → MP4 + JSON metadata → ffmpeg frames → Vault note + Skill
Fallback: Playwright 4-context parallel → 8 frames + caption → Vault note + Skill
```

## Current State
- **Completed**: 120 reels (vault notes + Hermes skills created)
- **Remaining**: 94 reels (all zero-auth Method B candidates)
- **Queue**: Next 10: C-3VtjDyNCA, DDzMI60sBvM, DDo8k0hRiWE, DDF9Ahzpqtp, DDQCeruq7ws, DCUb-vFvooS, DC1iY8LvN3Y, C-FNBHuIECz, C9urS8rIia_, DCVfuOBIdTp
- **Output Path**: `/Volumes/Samsung LED/Instagram Downloads/`

## Command History (Working)
```bash
# Test yt-dlp nightly
pip install -U --pre yt-dlp
yt-dlp --cookies cookies.txt --skip-download --print-json "https://instagram.com/reel/CODE/"

# Frame extraction
ffmpeg -i reel.mp4 -vf "select='eq(n,0)+eq(n,14%)+...'" frames/frame_%02d.png

# GIF creation
ffmpeg -framerate 2 -i frames/frame_%02d.png -vf palettegen palette.png
ffmpeg -framerate 2 -i frames/frame_%02d.png -i palette.png -lavfi paletteuse reel.gif
```

## Files Created This Session
- `scripts/run_pipeline.py` — Full pipeline script (yt-dlp primary, ffmpeg frames, vault + skill gen)
- `references/instagram-rate-limits.md` — Rate limit reference
- This file: `references/session-20260719.md`

## Next Steps
1. Test yt-dlp nightly on 5 remaining reels with fresh cookies.txt
2. If successful: run `scripts/run_pipeline.py` for all 94
3. If partial: implement Playwright 4-context fallback for failures