# Apple Mail Unified Inbox Observations

## Session Notes (2026-07-13)

- User prefers **direct Apple Mail access** over Outlook computer use for speed and simplicity.
- User wants **unified inbox management** across Exchange, Google, iCloud without duplicates.
- AppleScript queries to Mail.app can retrieve account names and unread subjects.
- Direct `enabled` property access fails; use `account type` to differentiate account types.
- Outlook.com (Exchange) requires fresh app passwords; verify validity before use.
- `computer_use` skill still functional but slower; Apple Mail offers faster interaction.
- User dislikes duplicate email forwarding; prefers native Mail.app handling.